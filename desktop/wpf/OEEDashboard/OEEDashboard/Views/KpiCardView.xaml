<UserControl x:Class="OEEDashboard.Views.KpiCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="220">
    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="6" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding Title}"
                       Style="{StaticResource MutedTextStyle}"
                       FontSize="13"
                       FontWeight="SemiBold" />

            <TextBlock Grid.Row="1"
                       Text="{Binding Value}"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{Binding Percent, Converter={StaticResource PercentToBrushConverter}}"
                       Margin="0,4,0,0" />

            <TextBlock Grid.Row="2"
                       Text="{Binding Subtitle}"
                       Style="{StaticResource MutedTextStyle}"
                       FontSize="12"
                       TextWrapping="Wrap" />

            <Border Grid.Row="4"
                    Background="#E5E7EB"
                    CornerRadius="3"
                    Height="6">
                <Border Background="{Binding Percent, Converter={StaticResource PercentToBrushConverter}}"
                        CornerRadius="3"
                        HorizontalAlignment="Left"
                        Width="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualWidth}">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleX="{Binding Percent, Converter={StaticResource PercentScaleConverter}}"
                                        ScaleY="1"
                                        CenterX="0"
                                        CenterY="0.5" />
                    </Border.RenderTransform>
                </Border>
            </Border>
        </Grid>
    </Border>
</UserControl>
