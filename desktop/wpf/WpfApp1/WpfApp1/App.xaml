<Application x:Class="WpfApp1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             xmlns:converters="clr-namespace:WpfApp1.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- React의 global CSS / theme variables와 유사한 앱 전역 리소스 -->

        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#F4F6F8" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="BorderBrush" Color="#DEE2E6" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0D6EFD" />
        <SolidColorBrush x:Key="PrimaryForegroundBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="MutedForegroundBrush" Color="#6C757D" />
        <SolidColorBrush x:Key="DangerBrush" Color="#DC3545" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#198754" />

        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter" />
        <converters:TaskStatusLabelConverter x:Key="TaskStatusLabelConverter" />
        <converters:PriorityToBrushConverter x:Key="PriorityToBrushConverter" />

        <!-- Style ≈ CSS class. TargetType으로 적용 대상 컨트롤 지정 -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>

        <Style x:Key="TagStyle" TargetType="Border">
            <Setter Property="Background" Value="#E9ECEF" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="6,2" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="#212529" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Padding" Value="12" />
            <Setter Property="MinWidth" Value="100" />
        </Style>
    </Application.Resources>
</Application>
