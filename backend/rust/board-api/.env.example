DATABASE_URL=sqlite://board.db?mode=rwc
HOST=127.0.0.1
PORT=3000
RUST_LOG=board_api=debug,tower_http=debug,sqlx=warn

# Docker (docker compose) — compose가 HOST/DATABASE_URL을 덮어씁니다.
# HOST=0.0.0.0
# DATABASE_URL=sqlite:///data/board.db?mode=rwc
