# Northstar — Mastro showcase

Mastro의 HTML tagged template, 함수형 컴포넌트, 파일 기반 라우팅과 제로-JavaScript 기본값을 한 번에 보여주는 에디토리얼 사이트입니다.

```sh
npm install
npm run dev
```

페이지는 평범한 `.mjs` 모듈입니다. 별도의 컴파일 문법 없이 데이터와 함수형 컴포넌트를 조합하며, 브라우저에는 완성된 HTML과 CSS만 전달됩니다.
